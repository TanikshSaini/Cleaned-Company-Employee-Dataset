# Cleaned-Company-Employee-Dataset
Cleaned Company Employee dataset using Pandas — inspecting and resolving missing values, inconsistent entries, incorrect data types, and duplicate records with imputation, normalization, and deduplication techniques. Final dataset exported as a clean CSV ready for analysis.
